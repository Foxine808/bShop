{
  "lint": {
    "rules": {
      // "no-explicit-any": true,
      // "require-await": true,
      // "no-empty": true,
      // "explicit-function-return-type": true,
      // "prefer-const": true,
      // "semicolon": ["error", "always"]  // Enforce semicolons
      "tags": ["recommended"],
      "include": ["prefer-const"]
      // "exclude": ["no-unused-vars"]
    }
  },
  "fmt": {
    "semiColons": true,
    "singleQuote": true
  },
  "tasks": {
    "dev": "deno run --watch main.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@surrealdb/surrealdb": "jsr:@surrealdb/surrealdb@^1.0.6"
  }
}
